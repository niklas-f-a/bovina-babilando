export enum AuthFrom {
  GITHUB_ID = 'GITHUB',
  MY_ID = 'MY_ID',
}

export enum ClientTokens {
  AUTH = 'AUTH_CLIENT',
  USER = 'USER_CLIENT',
}

export enum RabbitQueue {
  AUTH = 'auth',
  USER = 'user',
}
