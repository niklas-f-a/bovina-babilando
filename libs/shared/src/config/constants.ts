export enum ClientTokens {
  AUTH = 'AUTH_CLIENT',
  USER = 'USER_CLIENT',
}

export enum RabbitQueue {
  AUTH = 'auth',
  USER = 'user',
}

export enum ServiceTokens {
  AUTH = 'AUTH_SERVICE',
  USER = 'USER_SERVICE',
}
