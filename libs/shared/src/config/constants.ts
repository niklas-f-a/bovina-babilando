export enum AuthFrom {
  GITHUB_ID = 'GITHUB',
  MY_ID = 'MY_ID',
}

export enum ClientTokens {
  AUTH_SERVICE = 'AUTH_SERVICE',
  USER = 'USER_CLIENT',
}

export enum RabbitQueue {
  AUTH = 'auth',
  USER = 'user',
}
